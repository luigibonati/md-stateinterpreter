name: docs
channels:
dependencies:
  # Base depends
  - python
  - pip

  # Package dependencies
  - pandas
  - numpy
  - scipy
  - scikit-learn
  - matplotlib
  - mdtraj
  - nglview
  - cython
  - nbsphinx

  # Pip-only installs
  - pip:
    - tdqm
    - group-lasso
    - sphinx-copybutton  
    - furo

