name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - pandas
  - numpy
  - scipy
  - matplotlib
  - mdtraj

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - tdqm
